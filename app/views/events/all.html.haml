%h1 All Events
- @e.each do |e|
  .event
    %h2= link_to e.title, event_path(e)
    .location
      = e.location
    .time
      = e.event_start.strftime("%m/%d, %l:%M %p")
      = e.event_end.strftime("- %l:%M %p")
    .description
      = e.description
- if current_user
  = link_to 'Create New Event', new_event_path
