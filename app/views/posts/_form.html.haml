= form_for @p,  :html => { :multipart => true } do |f|
  - if @p.errors.any?
    .error_messages
      %h2 Form is invalid
      %ul
        - for message in @p.errors.full_messages
          %li= message
  %p
    #label
      = f.label :title
    = f.text_field :title
  %p
    #label
      = f.label :content
    %br/
    = f.text_area :content
  %p.button= f.submit
